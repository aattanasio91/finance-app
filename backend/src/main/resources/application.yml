spring:
  application:
    name: finance-app

  profiles:
    active: dev

server:
  port: 8080

app:
  jwt:
    secret: ${JWT_SECRET:default-secret-change-in-production}
    expiration: ${JWT_EXPIRATION:86400000}
